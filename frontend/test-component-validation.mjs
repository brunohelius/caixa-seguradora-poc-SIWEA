import puppeteer from 'puppeteer';
import fs from 'fs';

console.log('🧩 Starting Component Validation Tests\n');

const browser = await puppeteer.launch({
  headless: true,
  args: ['--no-sandbox', '--disable-setuid-sandbox']
});

try {
  const page = await browser.newPage();
  await page.setViewport({ width: 1920, height: 1080 });

  console.log('📄 Loading application...');
  await page.goto('http://localhost:5173/claims/search', {
    waitUntil: 'networkidle0',
    timeout: 30000
  });

  let results = [];

  // Test 1: Button components
  console.log('\n🔘 Testing Button components...');
  const buttons = await page.$$('button');
  const buttonCount = buttons.length;
  results.push(`Buttons: ${buttonCount > 0 ? `✅ ${buttonCount} found` : '❌ None found'}`);

  // Test 2: Input components
  console.log('📝 Testing Input components...');
  const inputs = await page.$$('input:not([type="hidden"])');
  const inputCount = inputs.length;
  results.push(`Inputs: ${inputCount > 0 ? `✅ ${inputCount} found` : '❌ None found'}`);

  // Test 3: Label components
  console.log('🏷️ Testing Label components...');
  const labels = await page.$$('label');
  const labelCount = labels.length;
  results.push(`Labels: ${labelCount > 0 ? `✅ ${labelCount} found` : '❌ None found'}`);

  // Test 4: Card components
  console.log('🗂️ Testing Card components...');
  const cards = await page.$$('[class*="card"], [class*="rounded"][class*="border"]');
  const cardCount = cards.length;
  results.push(`Cards: ${cardCount > 0 ? `✅ ${cardCount} found` : '❌ None found'}`);

  // Test 5: Table components
  console.log('📊 Testing Table components...');
  const tables = await page.$$('table');
  const tableCount = tables.length;
  results.push(`Tables: ${tableCount > 0 ? `✅ ${tableCount} found` : '❌ None found'}`);

  // Test 6: RadioGroup components
  console.log('⭕ Testing RadioGroup components...');
  const radios = await page.$$('input[type="radio"]');
  const radioCount = radios.length;
  results.push(`Radio buttons: ${radioCount > 0 ? `✅ ${radioCount} found` : '❌ None found'}`);

  // Test 7: Keyboard navigation
  console.log('\n⌨️ Testing keyboard navigation...');
  const focusableElements = await page.$$('button, input, select, textarea, a[href], [tabindex]:not([tabindex="-1"])');
  const focusableCount = focusableElements.length;
  results.push(`Focusable elements: ${focusableCount > 0 ? `✅ ${focusableCount} found` : '❌ None found'}`);

  // Test 8: ARIA attributes
  console.log('♿ Testing accessibility attributes...');
  const ariaLabels = await page.$$('[aria-label], [aria-labelledby]');
  const ariaCount = ariaLabels.length;
  results.push(`ARIA labels: ${ariaCount > 0 ? `✅ ${ariaCount} found` : '❌ None found'}`);

  // Navigate to dashboard
  console.log('\n📄 Testing Dashboard page...');
  await page.goto('http://localhost:5173/dashboard', {
    waitUntil: 'networkidle0',
    timeout: 30000
  });

  // Test 9: Progress components
  console.log('📈 Testing Progress components...');
  const progressBars = await page.$$('[role="progressbar"], [class*="progress"]');
  const progressCount = progressBars.length;
  results.push(`Progress bars: ${progressCount > 0 ? `✅ ${progressCount} found` : '❌ None found'}`);

  // Test 10: Badge components
  console.log('🏆 Testing Badge components...');
  const badges = await page.$$('[class*="badge"]');
  const badgeCount = badges.length;
  results.push(`Badges: ${badgeCount > 0 ? `✅ ${badgeCount} found` : '❌ None found'}`);

  // Generate report
  const report = `# Component Validation Report

**Test Date**: ${new Date().toISOString()}
**Test Environment**: http://localhost:5173/

## Component Test Results

${results.map(r => `- ${r}`).join('\n')}

## shadcn/ui Component Summary

✅ **Found Components:**
${results.filter(r => r.includes('✅')).map(r => `- ${r.split(':')[0]}`).join('\n')}

❌ **Missing Components:**
${results.filter(r => r.includes('❌')).length > 0 ?
  results.filter(r => r.includes('❌')).map(r => `- ${r.split(':')[0]}`).join('\n') :
  'None - All components found!'}

## Overall Status
${results.filter(r => r.includes('❌')).length === 0 ?
  '✅ All component types detected successfully!' :
  `⚠️ ${results.filter(r => r.includes('❌')).length} component types need attention`}

---
*Generated by Component Validation Test Suite*
`;

  fs.writeFileSync('component-validation-report.md', report);
  console.log('\n✅ Report saved to component-validation-report.md');
  console.log('\n📊 Test Results:');
  results.forEach(r => console.log(`  ${r}`));

} catch (error) {
  console.error('❌ Test suite error:', error);
} finally {
  await browser.close();
}